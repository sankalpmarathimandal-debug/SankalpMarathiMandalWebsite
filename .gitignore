# Dependencies (regenerate with `npm install` inside admin-worker/)
node_modules/

# Cloudflare Wrangler local state
.wrangler/

# Python bytecode cache
__pycache__/
*.pyc

# macOS
.DS_Store

# Local-only secrets for `wrangler dev` (never commit real tokens/passwords)
.dev.vars
